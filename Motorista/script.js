

function motorista (idadeDoUsuario, idadeDirigir) {

    if (idadeDoUsuario >= idadeDirigir) {
        document.write ("Você pode dirigir " + nome)

    } else {
        document.write ("Você não pode dirigir " + nome)
    }

}

var nome = "Marcelo";
motorista (30, 18);

